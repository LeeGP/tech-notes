---
title: 在这里写标题
description: 一句话摘要，会出现在首页列表里
pubDate: 2026-07-21
tags:
  - 技术方案
draft: false
---

> 使用方法：复制本文件到 `src/content/blog/`，改成新的文件名（英文短横线，如 `redis-cache.md`），再改下面的 frontmatter 与正文。改完后本地可 `npm run dev` 预览，再 `git push` 发布。

## 背景

当时要解决什么问题？上下文是什么？

## 方案

最终怎么做的？关键步骤或架构说明。

## 关键代码（可选）

```ts
// 只贴关键片段，不要整仓粘贴
export function example() {
  return true;
}
```

## 取舍

为什么不选别的方案？有哪些代价？

## 复盘

上线或实践后发现了什么？下次怎么改进？
